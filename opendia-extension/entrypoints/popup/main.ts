// Phase 1 popup entrypoint. Imports the legacy popup.js body verbatim; the
// popup UI still exists in v1 and will be removed in Phase 3.
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — legacy JS with no types.
import '../../src/popup/popup.js';
